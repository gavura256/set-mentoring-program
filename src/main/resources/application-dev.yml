spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:h2:mem:appDb;DB_CLOSE_DELAY=-1;MODE=MySQL;INIT=CREATE SCHEMA IF NOT EXISTS appDb
    username: sa
    password: password
    name: appDb
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: false
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  file:
    name: app.log
