Run tests and generate a JaCoCo HTML coverage report, then open it.

```bash
./mvnw verify
```

After the build, the report is at `target/site/jacoco/index.html`.
Summarise overall line and branch coverage percentages from the JaCoCo output.